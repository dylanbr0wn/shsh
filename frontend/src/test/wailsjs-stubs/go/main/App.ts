// Stub — replaced by vi.mock in setup.ts at runtime.
export const GetConfig = (): Promise<Record<string, unknown>> => Promise.resolve({})
export const SetDebugLevel = (): Promise<void> => Promise.resolve()
export const UpdateConfig = (): Promise<void> => Promise.resolve()
